# SFML Example

Example for SFML.

## Dependencies

Install dependencies:

```bash
sudo apt-get install libsfml-dev
```

## Compile

``` bash
$ mkdir cmake-build-debug
$ cd cmake-build-debug
$ cmake ..
$ make
```
